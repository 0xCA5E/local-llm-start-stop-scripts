#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PREFIX_DIR="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

if [[ -f "${PREFIX_DIR}/libexec/Install Dependencies.command" ]]; then
  TARGET_SCRIPT="${PREFIX_DIR}/libexec/Install Dependencies.command"
else
  TARGET_SCRIPT="${PREFIX_DIR}/Install Dependencies.command"
fi

exec /bin/bash "$TARGET_SCRIPT" "$@"
